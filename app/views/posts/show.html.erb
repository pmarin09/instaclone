<%= image_tag @post.pic, class: "my_pic" %>
<p><%= @post.caption %></p>
<%= link_to @post.user.email, @post.user %>


<%= button_to "Delete", @post, method: :delete, data: {confirm: "Are you sure?"}, class: "button" %>
